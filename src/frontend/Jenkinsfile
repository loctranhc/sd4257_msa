pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script{
                    sh 'cd /src/fontend'
                    sh 'docker build . -t frontend'
                }
            }
        }
        stage('Push Image to ACR') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}